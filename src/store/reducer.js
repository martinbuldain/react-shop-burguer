import * as actionTypes from './actions';

/**
 * I start converting the BurguerBuilder state to Redux
 */

const initialState = {
    ingredients: null,
    totalPrice: 4
};

const reducer = (state = initialState, action) => {

}

export default reducer;